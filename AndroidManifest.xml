<?xml version="1.0" encoding="UTF-8"?>
<manifest package="name.vampidroid" xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="12" android:versionName="1.12">
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
        <activity android:label="@string/app_name" android:name=".VampiDroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
				<action android:name="android.intent.action.SEARCH" />
				<category android:name="android.intent.category.DEFAULT" />
			</intent-filter>
			<meta-data android:name="android.app.default_searchable"
						 android:value=".VampiDroid" />
			<meta-data android:name="android.app.searchable" 
						 android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".CryptDetails"/>
        <activity android:name=".LibraryDetails"/>
        <activity android:name=".About"/>
        <activity android:name="EditPreferences"></activity>
        <provider android:authorities="name.vampidroid.VampidroidSuggestionProvider" android:name="VampidroidSuggestionProvider"></provider>
    </application>
    <uses-sdk android:minSdkVersion="7"/>
</manifest>
